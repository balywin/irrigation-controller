Sometimes when uploading a firmware file for OTA update and the WiFi connection is not stable, the upload process is interrupted as the connection is dropped, but 100% is shown and no actual update happens.
This scenario must be handled by the firmware and upload process must resume when the connection comes alive next.

